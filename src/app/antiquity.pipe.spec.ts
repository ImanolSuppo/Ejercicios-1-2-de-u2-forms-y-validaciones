import { AntiquityPipe } from './antiquity.pipe';

describe('AntiquityPipe', () => {
  it('create an instance', () => {
    const pipe = new AntiquityPipe();
    expect(pipe).toBeTruthy();
  });
});
